import { LatinOnlyDirective } from './latin-only.directive';

describe('LatinOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new LatinOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
